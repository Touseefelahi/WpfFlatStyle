<Window x:Class="FlatStyle.Sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlatStyle.Sample"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        WindowStyle="None"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundMainBrush}" />
        </Style>
        <Style TargetType="{x:Type local:MainWindow}">
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type local:MainWindow}">
                        <Grid>
                            <Border Background="{DynamicResource PrimaryBrush}">
                                <ContentPresenter Content="{TemplateBinding Content}" />
                            </Border>
                            <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Title}"
                               VerticalAlignment="Top" HorizontalAlignment="Left" Foreground="{DynamicResource ControlForegroundBrush}" FontWeight="Black"
                               Margin="12,8,0,0" />
                            <Image Source="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Icon}" VerticalAlignment="Top" HorizontalAlignment="Left" WindowChrome.IsHitTestVisibleInChrome="True" />
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right">
                                <Button Content="_" Style="{StaticResource WindowButton}" Click="MinimizeWindow" />
                                <Button Content="☐" Style="{StaticResource WindowButton}" Click="MaximizeWindow" />
                                <Button Content="X" Style="{StaticResource WindowButton}" Click="CloseWindow" />
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="0,28,0,0" Background="{DynamicResource BackgroundBrush}">
        <StackPanel HorizontalAlignment="Center">
            <Button Content="Switch Theme" Click="Button_Click" HorizontalAlignment="Center" Width="100" Height="30"  VerticalAlignment="Center" />
            <ComboBox>
                <ComboBoxItem IsSelected="True" Content="Item 1" />
                <ComboBoxItem Content="Item 2" />
                <ComboBoxItem Content="Item 3" />
            </ComboBox>
            <CheckBox IsChecked="True" Style="{StaticResource TickCheckBox}" />
            <DataGrid x:Name="datagrid" Height="150" />
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Primary Color" />
                <xctk:ColorPicker Name="ClrPcker_Primary" SelectedColorChanged="ClrPcker_Background_SelectedColorChanged"></xctk:ColorPicker>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Secondary Color" />
                <xctk:ColorPicker Name="ClrPcker_Secondary" SelectedColorChanged="ClrPcker_Secondary_SelectedColorChanged"></xctk:ColorPicker>
            </StackPanel>

            <Button Style="{StaticResource IconButton}" Content="&#xf086;" Width="50" Height="50" />

            <Button Style="{StaticResource IconButton}" Content="{StaticResource Fa-address-book}" Width="50" Height="50" />

            <Button Style="{StaticResource IconButton}" Content="{StaticResource Fa-address-card}" Width="50" Height="50" />
        </StackPanel>
    </Grid>
</Window>